name: Build Android APK (v1.14[beta])

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout repository code
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # 2. Setup Node.js environment
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: mobile/package.json

      # 3. Setup Java Development Kit (JDK) for Android build
      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 4. Install dependencies inside mobile folder
      - name: 📦 Install Dependencies
        working-directory: mobile
        run: npm ci

      # 5. Install Expo and EAS CLI globally
      - name: 🛠️ Install EAS CLI
        run: npm install -g eas-cli

      # 6. Setup Expo Credentials Token (For EAS services / updates matching)
      - name: 🔑 Setup Expo Token
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # 7. Compile APK using EAS Build (Cloud Runner)
      # Nota: Se compila usando el profile 'preview' para generar el archivo APK instalable.
      - name: 🚀 Build APK via EAS
        working-directory: mobile
        run: eas build --platform android --profile preview --non-interactive
